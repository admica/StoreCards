---
phase: 02-ux-quality-and-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/Barcode.tsx
  - app/components/SubmitButton.tsx
  - app/components/ToasterWithTheme.tsx
  - app/lib/validation.ts
  - app/layout.tsx
  - app/dashboard/page.tsx
autonomous: true
requirements: [BUG-04, UX-03, UX-04, UX-05]

must_haves:
  truths:
    - "When bwip-js fails to render a barcode, the raw barcode number is shown with a copy button instead of a blank canvas"
    - "Copying the barcode number puts it on the clipboard with visual confirmation"
    - "A user with no cards sees an illustration and 'Add your first card' CTA"
    - "Toast notifications appear at top-center, respect dark/light mode, and do not collide with BottomNav"
    - "A shared SubmitButton component exists with spinner + pending label pattern"
  artifacts:
    - path: "app/components/Barcode.tsx"
      provides: "Barcode render with error fallback showing raw value + copy button"
      contains: "navigator.clipboard.writeText"
    - path: "app/components/SubmitButton.tsx"
      provides: "Shared submit button with useFormStatus pending state"
      exports: ["SubmitButton"]
    - path: "app/components/ToasterWithTheme.tsx"
      provides: "Sonner Toaster wired to ThemeContext"
      exports: ["ToasterWithTheme"]
    - path: "app/lib/validation.ts"
      provides: "Zod client schemas for login, register, and card forms"
      exports: ["loginSchema", "registerSchema", "cardSchema", "validateField"]
    - path: "app/layout.tsx"
      provides: "Root layout with ToasterWithTheme inside ThemeProvider"
      contains: "ToasterWithTheme"
    - path: "app/dashboard/page.tsx"
      provides: "Enhanced empty state with refined copy and CTA"
      contains: "Add your first card"
  key_links:
    - from: "app/components/ToasterWithTheme.tsx"
      to: "app/providers/theme-provider.tsx"
      via: "useTheme() hook"
      pattern: "useTheme.*theme"
    - from: "app/layout.tsx"
      to: "app/components/ToasterWithTheme.tsx"
      via: "import inside ThemeProvider children"
      pattern: "ToasterWithTheme"
---

<objective>
Create the shared foundation components and install sonner for Phase 2 UX polish. This plan produces: (1) Barcode.tsx error state with raw number + copy button (BUG-04), (2) shared SubmitButton component (UX-04), (3) ToasterWithTheme + sonner install (UX-05), (4) Zod client validation schemas (UX-02 prep), and (5) enhanced dashboard empty state (UX-03).

Purpose: Downstream plans (02, 03) consume these shared components when wiring validation, toasts, and submit buttons into individual forms. Building them first avoids duplication and ensures consistent patterns.
Output: 4 new files (SubmitButton.tsx, ToasterWithTheme.tsx, validation.ts, package.json updated), 2 modified files (Barcode.tsx, layout.tsx, dashboard/page.tsx)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ux-quality-and-error-handling/02-RESEARCH.md

@app/components/Barcode.tsx
@app/layout.tsx
@app/dashboard/page.tsx
@app/providers/theme-provider.tsx
@app/login/page.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/providers/theme-provider.tsx:
```typescript
type Theme = 'light' | 'dark'
export function useTheme(): { theme: Theme; setTheme: (theme: Theme) => void }
```

From app/login/page.tsx (LoginButton pattern to generalize):
```typescript
function LoginButton() {
    const { pending } = useFormStatus()
    return (
        <button type="submit" disabled={pending}
            className="flex w-full justify-center items-center gap-2 rounded-xl bg-gradient-to-r from-accent to-accent-light px-4 py-3.5 text-sm font-semibold text-white shadow-lg shadow-accent/25 hover:shadow-accent/40 hover:scale-[1.02] ...">
            {pending ? (<><svg animate-spin .../> Signing in...</>) : 'Sign in'}
        </button>
    )
}
```

From app/components/Barcode.tsx (current — no error handling):
```typescript
export default function Barcode({ value, format }: { value: string; format: string })
// Uses bwipjs.toCanvas() in useEffect, catches error but only console.error
```

Existing input class pattern (for validation.ts docs):
```
"block w-full rounded-xl border border-border dark:border-border bg-background dark:bg-surface-elevated px-4 py-3 text-primary placeholder-muted shadow-sm focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent/20 transition-all text-sm"
```

Error input class pattern (for validation.ts docs):
```
"border-error focus:border-error focus:ring-error/20"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sonner, create shared components and validation schemas</name>
  <files>
    app/components/SubmitButton.tsx
    app/components/ToasterWithTheme.tsx
    app/lib/validation.ts
  </files>
  <action>
1. Run `npm install sonner` to add the toast library.

2. Create `app/components/SubmitButton.tsx` — a shared submit button using `useFormStatus` from `react-dom`:
   - Props: `label: string`, `pendingLabel?: string`, `className?: string`
   - When `pending` is true: show an SVG spinner (animate-spin, same as LoginButton) + `pendingLabel` (default: "Saving...")
   - When not pending: show `label`
   - Disable button when pending with `disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100`
   - Use the exact gradient button class from LoginButton: `flex w-full justify-center items-center gap-2 rounded-xl bg-gradient-to-r from-accent to-accent-light px-4 py-3.5 text-sm font-semibold text-white shadow-lg shadow-accent/25 hover:shadow-accent/40 hover:scale-[1.02] focus:outline-none focus:ring-2 focus:ring-accent focus:ring-offset-2 focus:ring-offset-background disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100 transition-all`
   - Allow `className` prop to append additional classes
   - Mark `'use client'`

3. Create `app/components/ToasterWithTheme.tsx` — wraps sonner's `<Toaster>` with theme from ThemeContext:
   - Import `useTheme` from `@/app/providers/theme-provider`
   - Import `Toaster` from `sonner`
   - Render `<Toaster theme={theme} position="top-center" richColors offset={16} toastOptions={{ duration: 4000, classNames: { toast: 'rounded-xl text-sm' } }} />`
   - Position MUST be `top-center` — BottomNav is fixed at bottom (~80px)
   - Do NOT use `theme="system"` — app manages theme via ThemeContext
   - Mark `'use client'`

4. Create `app/lib/validation.ts` — shared Zod schemas for client-side blur validation:
   - Import `z` from `'zod'` (NOT `'zod/v4'` — project uses standard import)
   - `loginSchema = z.object({ email: z.string().email('Enter a valid email address'), password: z.string().min(1, 'Password is required') })`
   - `registerSchema = z.object({ email: z.string().email('Enter a valid email address'), password: z.string().min(6, 'Password must be at least 6 characters') })`
   - `cardSchema = z.object({ retailer: z.string().min(1, 'Retailer name is required') })` — only retailer is required on card forms; barcodeValue and note are optional
   - Export a `validateField` helper: `(schema: z.ZodObject<any>, name: string, value: string) => string | null` — runs `schema.shape[name].safeParse(value)`, returns error message from `.error.issues[0].message` or `null` on success. Use Zod v4's `.issues` (not `.errors`).
  </action>
  <verify>
    <automated>cd /home/autopcap/storecard && node -e "require('sonner')" && test -f app/components/SubmitButton.tsx && test -f app/components/ToasterWithTheme.tsx && test -f app/lib/validation.ts && echo 'OK'</automated>
  </verify>
  <done>sonner installed, SubmitButton.tsx exports SubmitButton with spinner+pending, ToasterWithTheme.tsx exports themed Toaster, validation.ts exports loginSchema/registerSchema/cardSchema/validateField</done>
</task>

<task type="auto">
  <name>Task 2: Add Barcode error fallback, wire Toaster into layout, enhance empty state</name>
  <files>
    app/components/Barcode.tsx
    app/layout.tsx
    app/dashboard/page.tsx
  </files>
  <action>
1. Rewrite `app/components/Barcode.tsx` to add error state with copy fallback (BUG-04):
   - Add `useState<'idle' | 'success' | 'error'>('idle')` for render state tracking
   - Add `useState(false)` for `copied` state
   - In the existing `useEffect`, wrap `bwipjs.toCanvas()` in try/catch:
     - On success: `setState('success')`
     - On catch: `setState('error')` — do NOT log to console (it was silently failing before)
   - When `state === 'error'`, render a fallback div instead of the canvas:
     - Container: `flex flex-col items-center gap-2 p-4 bg-warning/10 border border-warning/20 rounded-xl`
     - Label: `<p className="text-xs text-muted">Barcode preview unavailable</p>`
     - Value: `<p className="font-mono text-sm font-medium text-primary">{value}</p>`
     - Copy button: calls `navigator.clipboard.writeText(value)` on click, shows "Copied" with checkmark for 2s then reverts to "Copy number". Use inline `copied` state toggle (not toast — per user discretion, inline checkmark fits better in this compact context)
     - Copy button class: `text-xs text-accent hover:text-accent-dark transition-colors flex items-center gap-1`
   - Wrap clipboard call in try/catch (fails in non-HTTPS non-localhost contexts)
   - When `state !== 'error'`, render the canvas as before
   - Preserve the existing props interface: `{ value: string; format: string }`
   - Handle edge case: if `value` or `format` is empty/falsy, stay in `idle` state (no render attempt)

2. Modify `app/layout.tsx` to add ToasterWithTheme:
   - Import `ToasterWithTheme` from `@/app/components/ToasterWithTheme`
   - Place `<ToasterWithTheme />` inside the `<ThemeProvider>` block, after `<BottomNav />`
   - Do NOT place it outside ThemeProvider — it needs useTheme() access

3. Enhance `app/dashboard/page.tsx` empty state (UX-03):
   - Change "No cards yet" heading to "No cards yet" (keep as is — it works)
   - Change button text from "Add First Card" to "Add your first card" to match success criteria
   - Change description to "Add your first loyalty card to start organizing your wallet." (minor: "Add" instead of current text if different)
   - The existing empty state already has: icon in accent circle, heading, description, gradient CTA button — this meets the "illustration and CTA" criteria. The SVG credit card icon IS the illustration.
   - No other changes needed — the existing empty state is substantively correct per research open question #2
  </action>
  <verify>
    <automated>cd /home/autopcap/storecard && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Barcode.tsx shows raw number + copy button when bwip-js fails (BUG-04), layout.tsx includes ToasterWithTheme inside ThemeProvider (UX-05), dashboard empty state says "Add your first card" (UX-03)</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -r "ToasterWithTheme" app/layout.tsx` confirms Toaster is wired
3. `grep "navigator.clipboard" app/components/Barcode.tsx` confirms copy button exists
4. `grep "Add your first card" app/dashboard/page.tsx` confirms empty state CTA
5. `node -e "const v = require('./app/lib/validation'); console.log(Object.keys(v))"` shows exported schemas
</verification>

<success_criteria>
- sonner is installed and ToasterWithTheme renders inside ThemeProvider in layout.tsx
- Barcode.tsx shows raw number + copy button when bwip-js fails (not blank canvas)
- SubmitButton.tsx is a shared component with spinner + configurable labels
- validation.ts exports loginSchema, registerSchema, cardSchema, validateField
- Dashboard empty state CTA reads "Add your first card"
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-ux-quality-and-error-handling/02-01-SUMMARY.md`
</output>
